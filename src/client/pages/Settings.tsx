import * as React from 'react';

interface Props { }
interface State { }

export class Settings extends React.Component<Props, State> {
    render() {
        return (
            <h1>Settings</h1>
        );
    }
}
